# 图片元素

## img元素

空元素

src属性：source（图片的路径）

alt属性：当图片资源失效时，将使用该属性文字替代图片

可用css的objeect-fit改变图片填充方式

## 和a元素连用

a元素里套一个img元素

## 和map元素连用

map:地图

map的子元素：area（shap:circle;rect;poiy）

衡量坐标时，为了避免衡量误差，需要专业的衡量工具

## 和figure元素连用

它的一个子元素：figcaption
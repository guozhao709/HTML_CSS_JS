# 多媒体元素

## video元素

controls属性（不用写属性值）:控制控件的显示

某些属性，只有两种状态：1.不写  2.取值为属性名，这种属性为布尔属性

eg:  autoplay（自动播放）  muted(静音播放)  loop（循环播放）

## audio元素

## 兼容性

1. 旧版本的浏览器不支持这两个元素
2. 不同的浏览器支持的音视频格式可能不一致（video元素套多个视频格式元素（source））

        mp4、webm


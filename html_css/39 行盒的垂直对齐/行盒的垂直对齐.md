# 行盒的垂直对齐

## 多个行盒垂直方向上的对齐

给没有对齐元素设置vertical-align(仅限**行盒**)

    预设值:middle(基线对齐) top,bottom等

    也可以是数值和百分比

## 图片的底部白边

图片的父元素是一个快盒,快盒高度自动,图片底部和父元素底边之间往往会出现空白

解决办法

1. 设置父元素的父元素字体大小为0
2. 将图片设置为块盒





